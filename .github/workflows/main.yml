name: Run pytest
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run pytest
        run: pytest
on:
  pull_request:
    branches:
    - main
